# Legacy Zotero MCP Setup

Zotero MCP setup instructions are external to this Pharmacy MCP package. Use
installed Zotero/PubMed MCP tools when available, but do not run nonexistent
local setup paths from this repository.
