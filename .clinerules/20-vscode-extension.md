# Legacy Extension Rules

This repository does not contain a VS Code extension. Any extension or VSIX
workflow belongs to an external repository and must not be run from Pharmacy
MCP unless that repository is explicitly opened.

Use the Pharmacy MCP Python release gates from `.clinerules/00-project.md`.
