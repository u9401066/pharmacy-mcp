# Legacy Zotero VS Code Extension Rules

This Pharmacy MCP repository has no VS Code extension package. Do not run npm,
VSIX packaging, or extension asset-sync commands here unless an external
extension repository is explicitly opened.

Use the Pharmacy MCP Python release gates from `.clinerules/00-project.md`.
