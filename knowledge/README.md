# Local pharmaceutical knowledge

Files placed in this directory are available to the default `file` provider.
Supported formats are Markdown/text, CSV, PDF, DOCX, XLSX, and XLS. Legacy DOC
files require the `antiword` executable.

Do not put PHI, secrets, or copyrighted content here unless repository access
and downstream agent access are authorized for that material.
